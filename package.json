{
  "name": "WPP-Booster-Sentimento",
  "version": "0.0.1",
  "description": "App para mostrar os níveis de sentimento com a voz",
  "keywords": "voice",
  "license": "",
  "author": "Rodrigo Barona",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "tailwindcss": "^1.9.6"
  }
}
